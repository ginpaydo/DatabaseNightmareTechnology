<UserControl x:Class="DatabaseNightmareTechnology.Views.OutputResultUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DatabaseNightmareTechnology.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- 画面分割の定義 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="10" Grid.Row="0">
            <Label Content="出力結果" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Top" FontFamily="Meiryo UI" FontSize="22" FontWeight="Bold"/>
            <ComboBox Name="OutputDataList" ItemsSource="{Binding OutputDataList}" IsSynchronizedWithCurrentItem="True" FontFamily="Meiryo UI" VerticalContentAlignment="Center"/>
        </StackPanel>

        <TextBox Grid.Row="1" Text="{Binding Body.Value}" TextWrapping="Wrap" AcceptsReturn="True" FontFamily="Meiryo UI" IsReadOnly="True"/>

        <StackPanel Grid.Row="2">
            <Button Content="削除" Command="{Binding Delete}" HorizontalAlignment="Right" Margin="0,0,10,10" Width="76" FontFamily="Meiryo UI">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Red" Offset="0"/>
                        <GradientStop Color="#FFFFDCDC" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
